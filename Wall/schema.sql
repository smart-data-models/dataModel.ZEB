/* (Beta) Export of data model Wall of the subject dataModel.ZEB for a PostgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE Wall_type AS ENUM ('Wall');CREATE TYPE wallType_type AS ENUM ('outer','inner','basement');
CREATE TABLE Wall (address JSON, alternateName TEXT, areaServed TEXT, dataProvider TEXT, dateCreated TIMESTAMP, dateModified TIMESTAMP, dateObjectCreated TIMESTAMP, dateObjectUpdated TIMESTAMP, description TEXT, flagDeleted TEXT, id TEXT PRIMARY KEY, jointArea NUMERIC, jointDirection JSON, jointSurface JSON, location JSON, name TEXT, objectName TEXT, owner JSON, refArea JSON, refBuilding JSON, refMaterialLayer JSON, refOpening JSON, refRoom JSON, refStair JSON, seeAlso JSON, shape JSON, source TEXT, surfaceInner JSON, surfaceOuter JSON, thermalTransmission NUMERIC, thickness NUMERIC, type Wall_type, volumetricSpecificHeat NUMERIC, wallType wallType_type);