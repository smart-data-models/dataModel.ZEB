/* (Beta) Export of data model Column of the subject dataModel.ZEB for a PostgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE Column_type AS ENUM ('Column');
CREATE TABLE Column (address JSON, alternateName TEXT, areaServed TEXT, dataProvider TEXT, dateCreated TIMESTAMP, dateModified TIMESTAMP, dateObjectCreated TIMESTAMP, dateObjectUpdated TIMESTAMP, description TEXT, flagDeleted TEXT, height NUMERIC, id TEXT PRIMARY KEY, jointArea NUMERIC, jointDirection JSON, jointSurface JSON, location JSON, name TEXT, objectName TEXT, owner JSON, refArea JSON, refBuilding JSON, refMaterial JSON, refRoom JSON, refStair JSON, seeAlso JSON, shape JSON, source TEXT, surfaceBase JSON, thermalTransmission NUMERIC, type Column_type, volumetricSpecificHeat NUMERIC);